const Core = artifacts.require('EcoAllyCore');

// contract('EcoAllyCore', function(accounts){
//     it('Should print first account', function(){
//         return Core.deployed().then(function(instance){
//             return instance.testInteraction.call(4);
//         }).then(function(val){
//             assert.equal(Number(val), 16, 'test interaction function did not yield the correct result');
//         })
//     });
// });